[package]
name = "oii"
version = "0.1.0"
edition = "2024"
description = "OII config format. Brackets for scope. Parser and formatter"
authors = ["Celvra"]
license = "Apache-2.0"

[dependencies]
chumsky = "0.9.3"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
